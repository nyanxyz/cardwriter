import { PaperCard } from "@/app/paper-card";

export function PaperCardSection() {
  return (
    <div className={"bg-gray-100 py-8 px-12 flex flex-col justify-center"}>
      <PaperCard />
    </div>
  );
}
